数据库操作

Mapper 负责执行具体的 SQL 语句，包括增删改查（CRUD）操作。

它通过接口方法定义数据库操作，具体的 SQL 语句可以通过注解或 XML 文件配置。

对象-关系映射（ORM）

Mapper 将数据库表中的记录映射为 Java 对象（POJO），或将 Java 对象映射为数据库记录。

解耦

Mapper 将 SQL 操作与业务逻辑分离，使代码更清晰、更易于维护。

灵活性

通过 MyBatis 的 XML 配置或注解，可以灵活地编写复杂的 SQL 语句。